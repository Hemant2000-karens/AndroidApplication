package com.apple.ippb;

import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;

public class Biometrics_replace extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        //setContentView(R.layout.activity_biometrics_replace);
/*
        ImageButton bio_h = findViewById(R.id.Bio_back);
        bio_h.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                Intent i = new Intent(getApplicationContext(),HomeP.class);
                finish();
                i.putExtra("name_vale",MainActivity.ID());
                startActivity(i);
            }
        });

 */













    }
}