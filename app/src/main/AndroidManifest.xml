<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apple.ippb">
    <uses-permission android:name="android.permission.CAMERAA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ScanActivity" />
        <activity android:name=".ProfileUpdate" />
        <activity android:name=".SignUp" />
        <activity android:name=".verifyotp" />
        <activity android:name=".AddRecords" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ForgetPassword" />
        <activity android:name=".Biometrics_replace" />
        <activity android:name=".Sim_replace" />
        <activity android:name=".Mobile_Replace" />
        <activity android:name=".Replace_device" />
        <activity android:name=".Isue_dev" />
        <activity android:name=".HomeP" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>